# Calculando Descontos
valor = float(input('Digite o valor do produto: R$'))
print(f'O produto, cujo valor original era R${valor:.2f}, com o desconto de 5%, passa a custar R${valor*0.95:.2f}.')

# # Calculando Descontos
# preco = float(input('Digite o valor do produto: R$'))
# desc = preco*(1 - 5 / 100)
# print('O produto, cujo preço original era R${:.2f}, com o desconto de 5%, passa a custar R${:.2f}.'.format(preco, desc))
